# r3bl_tui v0.8.0 Release Notes & Migration Guide

| ⚠️ **Breaking Release**  | `v0.8.0` introduces breaking API changes. Upgrading from `v0.7.x`? Jump to the [Migration Guide]. |
| :----------------------- | :------------------------------------------------------------------------------------------------ |
| 🏃 **Interactive Demos** | Test drive 16 examples, jump to [Interactive Demos].                                              |
| 📦 **Install & Links**   | `cargo add r3bl_tui` · [crates.io] · [docs.rs]                                                    |

## Modernizing the Terminal: Taking Inspiration From Web and Desktop Apps

Despite the massive rise of AI/LLM coding agents & execution harnesses and cloud VM
administration over SSH, terminal UI innovation has largely stagnated since the 1970s.
Most CLI tools still rely on blocking single-threaded I/O, [curses]-era APIs, and fragile
platform hacks - or rely on the heavy, fragile workaround of layering web stacks like
[`Node.js`], [`React`], and [`ink`] onto the console. This introduces unreasonable memory
bloat, high latency, broken keyboard shortcuts, and unpredictable instability that breaks
down during long-horizon agentic workflows and sub-process orchestration.

**r3bl_tui moves the terminal forward (with love & respect) into 2026 and beyond. ❤️**

It brings the ergonomics of modern web development ([React], [flexbox], [CSS]) to the
terminal, turning it into a place of focused productivity to build delightful, ergonomic,
and rich text user interface (TUI) experiences.

Representing years of systems programming, performance optimization, and production-grade
infrastructure design in Rust, **r3bl_tui** re-imagines the terminal for the modern era -
making rich, reactive TUI applications accessible over SSH to any terminal emulator across
Linux, macOS, Windows, and Unix/BSDs (such as FreeBSD).

**r3bl_tui** is fundamentally different from [`vim`], [`neovim`], and [`ratatui`] through
its immediate mode reactive UI, clean separation between rendering and state mutation, and
purely async architecture - it never blocks the main thread, and natively embraces
multithreaded execution and multi-process orchestration.

### Primary Use Cases

`r3bl_tui` is designed to power four primary use cases:

- 🤖 **AI/LLM Coding Agents & Execution Harnesses**: Building fast, interactive terminal
  interfaces and execution harnesses for AI/LLM coding agents in pure Rust. While popular
  agent tools are written in [`Node.js`] and [`ink`], Node-based runtimes struggle with
  runaway memory consumption, fragile sub-process control, and unstable PTY session
  management when orchestrating heavy, long-running tools in sandboxed environments
  (containers, microVMs, systemd-nspawn machines, etc.). Furthermore, Node-based runtimes
  struggle with terminal input decoding, frequently dropping modifier keys, mangling
  keyboard chords, and introducing sluggish ESC key disambiguation lag. `r3bl_tui`
  provides a rock-solid, pure Rust foundation engineered for native PTY orchestration,
  zero-latency ANSI input decoding, deterministic process lifecycles, flicker-free diff
  rendering, and minimal resource overhead.

- ☁️ **DevOps & Cloud Workflows**: Administering modern cloud infrastructure shouldn't
  feel like using stone-age tools to build a satellite. Juggling remote VMs, containers,
  and multi-process server environments through bare terminal sessions is slow and
  error-prone. `r3bl_tui` transforms terminal workflows into a rich, responsive workspace
  with in-memory virtual terminal emulation, virtual tabs, 2D horizontal panning across
  wide logs, and double-buffered diff rendering that stays lag-free even over high-latency
  SSH connections.

- 📝 **Interactive Document & Markdown Workflows**: Rich interactive editing, syntax
  highlighting, and real-time code block execution directly inside Markdown documents in
  the terminal, bridging documentation directly with live operations.

- 🛠️ **Developer Productivity Infrastructure**: High-efficiency developer tooling and
  composable terminal infrastructure to enhance knowledge capture, eliminate friction, and
  streamline everyday command-line workflows.

### Built-from-Scratch Primitives: The Four Pillars of `r3bl_tui`

It is a fully async TUI framework and toolkit spanning the entire terminal spectrum,
providing four foundational interaction primitives designed from the ground up in pure
Rust:

1. 📟 **CLI & REPLs (`readline_async`)**: Unlike GNU [`readline`] which is single-threaded
   and blocking, our implementation is fully async, interruptable, and non-blocking,
   allowing background spinners and tasks to print concurrently without pausing line
   editing or blocking your main thread.

2. 📑 **Inline / Partial TUI (`choose`)**: Single-shot interactive multi-select dialogs
   that enter raw mode and render inline in terminal scrollback without taking over the
   screen or disrupting the back buffer (similar to `fzf` in spirit).

3. 🖥️ **Full-Screen TUI**: Complete raw mode with alternate screen support, fully async
   and panic-safe terminal restoration. Build [React] and [Elm] inspired apps with
   [unidirectional data flow], [responsive] [flexbox] layouts, [declarative] [CSS-like]
   styling, reactive state architecture, reusable modal dialogs with asynchronous
   autocomplete, and a full-featured Markdown editor component with custom parser, custom
   syntax highlighter, and fast zero-copy gap buffer.

4. 🔀 **Terminal Multiplexing & PTY**: In-memory virtual terminals, virtual terminal tabs,
   deterministic process orchestration, and multiplexing primitives (build your own
   [`tmux`] or sandboxed AI/LLM coding agent execution harness) featuring 2D horizontal
   viewport panning and decoupled throughput.

**Power via Composition**: Designed to be [loosely coupled and strongly coherent], you can
pick and choose only what you need or compose them seamlessly within a single application.
Transition smoothly from a non-blocking `readline_async` prompt into an inline `choose`
menu, launch a full-screen TUI for complex tasks, or orchestrate background PTY
subprocesses, all sharing application state without process restarts.

### Automated Headless Testing & Benchmarking

The entire framework across all 4 pillars is testable across Linux, macOS, and Windows.
Powered by our own PTY pillar, real production code runs headlessly in an isolated virtual
terminal environment, enabling end-to-end testing of interactive apps without requiring
human interaction:

- **PTY Automated Interactive Testing**: Enables end-to-end testing of interactive apps
  via headless PTY subprocess orchestration with the `generate_pty_test!` macro. Automate
  key sequences, window resizing, and screen output verification in completely isolated
  child processes without someone having to sit and manually type at a keyboard.
- **Visual UI Snapshot Testing**: Everything renders to an offscreen buffer (`OfsBuf`),
  providing built-in visual snapshot testing. Easily diff and assert the actual rendered
  terminal screen state headlessly generated in a real PTY environment.
- **Sans-IO In-Memory Terminal**: Pure functional VT-100 parser verifies virtual terminal
  screen buffers directly in RAM with zero OS syscalls.
- **Decoupled I/O Devices**: `InputDevice` and `OutputDevice` abstract terminal I/O away
  from physical `stdin` and `stdout`, allowing input events and output streams to be
  driven and inspected directly in tests without taking over your terminal.
- **Empirical Benchmarking & Flamegraph Profiling**: Continuous performance measurement
  via Rust's built-in benchmarking suites and automated flamegraph profiling against
  established baselines rather than guesswork.

### Engineered for Production Performance, Robustness, Correctness and Reliability

- 📐 **Zero-Cost Type-Proof Architecture & Mathematical Correctness**: Rather than relying
  on ambiguous primitive integers (`usize`, `u16`) and runtime assertions, formal type
  theory and trait hierarchies [make illegal states unrepresentable]:
    - **The Newtype Pattern for Domain Separation & Coordinate Safety**: Replaces raw
      primitive integers with zero-cost newtypes (`CRow`, `VPRow`, `CHeight`), eliminating
      primitive obsession and transposition bugs with zero runtime memory or performance
      overhead:
        - **0-Index vs 1-Index Separation**: Strongly types the distinction between
          0-based buffer positions (`IndexOps`: `VPRow`, `VPCol`, `CRow`, `CCol`), 1-based
          terminal coordinates (`TermRow`, `TermCol`), and 1-based lengths (`LengthOps`:
          `VPHeight`, `VPWidth`, `CHeight`, `CWidth`).
        - **Safe Bidirectional Conversions**: Pairing traits provide explicit conversions
          between 0-based and 1-based domains (`to_zero_based()`, `from_zero_based()`,
          `convert_to_length()`).
        - **Algebraic Identities & CSI Zero Protection**: Enforces algebraic laws
          (`Index + Length = Index`, `Index - Index = Length`) to eliminate dangerous
          manual arithmetic on raw integers, preventing off-by-one errors (`<` vs `<=`),
          negative underflow, and CSI zero-index terminal crashes (`TermRowDelta`,
          `TermColDelta`).
        - **Empirically Proven Zero-Cost Layout**: Newtypes share the identical memory
          layout, size, and ABI of raw primitives (passed in CPU registers with zero heap
          allocation or indirection) and are completely erased during LLVM compilation;
          Criterion benchmarks confirm execution differences remain strictly within the
          +/- 2% noise margin ([FUNARCH 2026 paper]).
    - **Dual-Domain Coordinate Trait Hierarchy**: Strict trait boundaries isolate 64-bit
      memory coordinates (`StorageCoordinate`, `usize`: `CPos`) from 16-bit visual
      coordinates (`ScreenCoordinate`, `u16`: `VPPos`), requiring explicit camera viewport
      transformations.
    - **Explicit Narrowing/Widening Traits & Zero Raw `as` Casts**: Replaces dangerous,
      silent primitive `as` casting across the codebase with explicit, type-safe traits
      (`WideningCastTo` for lossless promotions and `NarrowingCastTo` for checked/clamped
      reductions), preventing accidental truncations and sign-loss bugs.
    - **Eliminating Boolean Blindness with Witness Enums**: Grounded in ACM research
      ([FUNARCH 2023 paper] and [Parse, don't validate]), boundary and bounds checks never
      return uninformative `bool` flags. Instead, they return structured witness enums
      (`ArrayOverflowResult`, `RangeBoundsResult`), forcing callers to exhaustively handle
      all boundary states at compile time.

- 🦄 **First-Class Unicode & Complex Emoji Engine (`GCString`)**: Most terminal emulators
  and TUI libraries break when handling "jumbo emojis", zero-width joiners (ZWJ), skin
  tone modifiers, and wide characters (display width > 1), causing visual tearing,
  misaligned borders, and string-slicing panics. ROC solves this from the ground up via
  `GCStringOwned`:
    - **Tri-Index Separation**: Strictly decouples memory position (`ByteIndex`, UTF-8
      offset), logical editing position (`SegIndex`, user-perceived grapheme clusters),
      and visual column position (`VPCol`, actual terminal display width).
    - **Complex Emoji & Modifier Support**: Correctly measures and renders multi-codepoint
      sequences (e.g., `👨🏾‍🤝‍👨🏿` spans 5 codepoints and 7 code units, but resolves to 1 logical
      grapheme segment and 2 visual columns).
    - **Panic-Proof Slicing & Boundary Safety**: Guarantees cursor navigation, backspace,
      and substring slicing never split UTF-8 codepoints or mid-grapheme clusters,
      eliminating runtime slicing panics across editors, line inputs, and diff rendering.

- 🔒 **Supply-Chain Integrity & Owning Our BOM**: In an era of software supply-chain
  attacks, maintainer burnout, and abandonware, our explicit architectural goal is to
  **own our Bill of Materials (BOM)**. Critical primitives (including our custom
  `direct_to_ansi` terminal I/O backend, VT-100/ANSI parser, zero-copy gap buffer, and
  Markdown parser) are engineered in-house in pure Rust. We strictly limit external
  dependencies to actively funded, strongly supported, and battle-tested industry
  foundations (such as `tokio`, `mio`, and `mimalloc`), safeguarding production
  applications against transitive bloat, sudden deprecations, and upstream
  vulnerabilities.

- 🌍 **Multi-Backend Architecture (Linux, macOS, Windows, Unix/BSDs)**: Native,
  first-class support across platforms using the best backend for each OS:
    - **Linux**: Our custom high-performance, Linux-native `direct_to_ansi` engine
      (talking directly to the terminal device via `mio`/epoll for minimal latency and
      maximum throughput, without the use of `crossterm`).
    - **macOS, Windows & Unix/BSDs**: Currently powered by `crossterm`. We plan to expand
      `direct_to_ansi` in the future to replace `crossterm` across all platforms.

- 🚀 **SIMD Contiguous Memory Layout (`Flat2DArray`)**: Single contiguous 1D allocation
  indexed as 2D, delivering 2.3x rendering speedups, SIMD chunk batching, and eliminating
  pointer indirection ([implementation deep
  dive][High-Performance Flat 2D Arrays in Rust (SIMD, L1 Cache)] and [memory latency
  theory][Rust, Memory performance & latency]).

- 🧑‍🤝‍🧑 **Double-Buffered SSH-Optimized Diff Rendering**: Double-buffered compositor computes
  minimal cell-level diffs between frames, painting only what changed for smooth,
  flicker-free performance over high-latency SSH connections, with multi-layer Z-order
  compositing for modal overlays and popups.

- 🔀 **Terminal Multiplexing & PTY Architecture (`PTYMux`)**: Decouples physical display
  constraints from subprocess execution via virtual terminals and process orchestration:
    - **2D Viewport Panning in PTY Sessions (Horizontal Scrolling without Wrapping)**:
      Traditional terminal emulators (`xterm`, `Alacritty`, `Kitty`) lack horizontal
      panning for standard CLI tools (`cat`, `grep`, `git log`, `dmesg`), forcibly
      hard-wrapping wide lines across rows and mangling tabular data, JSON, or stack
      traces into unreadable spaghetti. When running any CLI program inside a ROC PTY
      session (a concrete manifestation of which is [`pty_mux_example`], powered by
      [`GrowableBuffer`] and [`PTYMux`]), `r3bl_tui` decouples the physical viewport from
      the virtual terminal canvas width (e.g., 1,000+ columns). The child process writes
      into this wide virtual canvas without line wrapping, allowing users to smoothly pan
      horizontally (`Shift + Mouse Wheel` or trackpad gestures) across the output without
      layout destruction.
    - **Decoupled Throughput vs. Terminal Bottlenecks (Faster Than Bare `xterm`)**: In
      traditional terminal emulators (like `xterm`), running a command that dumps
      megabytes of text (e.g., `cat large.log`) blocks the process on `stdout` I/O while
      the terminal synchronously parses escape codes, recalculates line wrapping, and
      rasterizes every glyph. Counter-intuitively, running that same command inside a ROC
      PTY session (such as [`pty_mux_example`]) running _inside_ `xterm` is often
      significantly **faster than running the command directly in bare `xterm`**.
      `r3bl_tui`'s headless VT-100 parser (`OfsBufVT100`) acts as an in-memory shock
      absorber, ingesting raw subprocess output at memory bus speeds, while
      double-buffered diff rendering paints only the visible viewport to the host terminal
      at controlled display intervals. Subprocesses drain `stdout` without stalling on
      terminal drawing or network I/O backpressure.

- 🕊️ **Terminfo Liberation**: Completely frees your applications from legacy `terminfo` /
  `termcap` databases and [`ncurses`] baggage by querying modern ANSI protocols directly at
  runtime.

- 📜 **`CSS`-Like Styling & Declarative Layouts**: Responsive [flexbox] layouts and
  [declarative] [CSS-like] styling inspired by [React] and [Elm].

- 🎨 **Intelligent Color Degradation**: Automatically detects terminal capabilities and
  gracefully degrades colors: **24-bit Truecolor -> 256 colors -> 16 ANSI colors ->
  Monochrome** (black & white). Gracefully handles environments lacking truecolor support
  (such as pre-macOS 26 Tahoe `Terminal.app`, Linux virtual consoles, or headless CI
  runners), complete with dynamic lolcat rainbow color-wheel palettes that automatically
  adapt to terminal color capability.

- ⌨️ **Modern Terminal Input & Keyboard Protocol Architecture**: Combines Linux-native
  kernel TTY polling via `direct_to_ansi` with an IO-free Sans-IO protocol state machine
  for robust, zero-latency input handling across local terminals and SSH:
    - **Linux-Native `direct_to_ansi` Driver**: Bypasses `crossterm` and `libc` FFI
      wrappers on Linux by talking directly to `/dev/tty` via `mio`/`epoll(7)`,
      eliminating thread-blocking reads and CPU-spinning loops.
    - **Disambiguating Modifier Chords & Key Collisions**: Reliably decodes chords and
      combinations that traditional terminal runtimes mangle or drop - such as
      [`Shift+Enter`], `Ctrl+Enter`, `Ctrl+Tab`, `Ctrl+Number/Punctuation`, `Ctrl+I` vs
      `Tab`, and `Alt+[` collisions.
    - **Kitty Keyboard Protocol (`CSI u`) Progressive Enhancement**: Negotiates advanced
      keyboard protocols with modern terminals ([Kitty keyboard protocol]), enabling full
      modifier reporting, key-release events, and unambiguous key sequences with graceful
      legacy fallback.
    - **Zero-Latency ESC Disambiguation (`MaybeMore`)**: Replaces brittle 50-100ms timeout
      heuristics with an internal state machine (`KernelDrained`, `KernelMayHaveMore`),
      achieving 0ms zero-latency ESC handling while correctly reassembling multi-packet
      escape sequences across SSH.
    - **OSC Terminal Query Absorption & SGR Mouse Reporting**: Frames and absorbs
      background terminal responses (such as OSC 10/11 color queries and OSC 52 clipboard)
      on `stdin` to prevent terminal text leakage, while supporting full SGR mouse
      tracking (clicks, drags, scroll wheel).

- 🧩 **Composable "Applet" Architecture & Shared State**: Enables multiple integrated TUI
  experiences ("applets") to run within the same process and terminal window. Supports
  shared application state across sub-apps alongside local view state, allowing seamless
  transitions and routing between Full-Screen TUI views and inline Partial-TUI dialogs
  without process restarts or lost context.

## Interactive Demos

You can browse and test drive 16 interactive demos across all 4 pillars in the
[`tui/examples`] directory:

- **CLI & REPL ([`readline_async`])**:
    - [`readline_async.rs`] - Asynchronous non-blocking line input with background tasks
    - [`readline_async_multiline.rs`] - Multiline line editing with async readline
    - [`spinner.rs`] - Concurrent async spinners and indeterminate progress indicators
    - [`shell_async.rs`] - Interactive async command execution shell
- **Inline / Partial TUI ([`choose`])**:
    - [`choose_interactive.rs`] - Single-shot interactive multi-select dialog
    - [`choose_quiz_game.rs`] - Interactive quiz game powered by choose
    - [`choose_with_and_without_readline_async.rs`] - Transitioning between readline and
      inline dialogs
- **Full-Screen Apps ([`tui_apps`])**:
    - [`ex_editor`] - Full-featured Markdown editor with syntax highlighting and gap
      buffer
    - [`ex_pitch`] - Presentation slide deck component with animations
    - [`ex_rc`] - Run command dialog with async autocomplete
    - [`ex_app_with_1col_layout`] - Responsive single-column flexbox layout
    - [`ex_app_with_2col_layout`] - Responsive two-column flexbox layout
    - [`ex_app_no_layout`] - Bare-bones reactive applet without layout engine
- **Terminal Multiplexing & PTY ([`PTYMux`])**:
    - [`pty_mux_example.rs`] - Full terminal multiplexer session with tabbed subprocesses
    - [`pty_2d_panning_example.rs`] - 2D horizontal viewport panning without text wrapping
    - [`pty_simple_example.rs`] - Minimal PTY process spawn and execution
    - [`pty_rw_echo_example.rs`] - Bidirectional PTY read/write communication
    - [`pty_mux_osc_demo.rs`] - Terminal query absorption and OSC response demonstration
    - [`spawn_pty_interactive.rs`] - Interactive child process session handling
    - [`spawn_pty_output_capture.rs`] - Headless PTY output streaming and capture
- **Diagnostic & Inspector Tools**:
    - [`mouse_inspector.rs`] - Live terminal mouse event inspection and SGR tracking

> 💡 **To run any demo locally**, clone the
> [ROC repository](https://github.com/r3bl-org/r3bl-open-core) and use `cargo`:
>
> ```bash
> cargo run -p r3bl_tui --example <example_name>
> # Examples:
> cargo run -p r3bl_tui --example readline_async
> cargo run -p r3bl_tui --example choose_interactive
> cargo run -p r3bl_tui --example pty_mux_example
> cargo run -p r3bl_tui --example tui_apps
> ```
>
> _Or use `fish run.fish run-examples` for an interactive menu with logging and release
> options; see [README](https://github.com/r3bl-org/r3bl-open-core#readme)._

### Learning Resources & Deep Dives

- 📺 **Build with Naz Video Playlists**:
    - [Build with Naz: TTY playlist]
    - [Build with Naz: async readline]
- 📖 **Architectural Deep Dives & Documentation**:
    - [API documentation on docs.rs]
    - [High-Performance Flat 2D Arrays in Rust (SIMD, L1 Cache)]
    - [Rust, Memory performance & latency]
    - [To async or not to async: Rust MCP server]

## Migration Guide from v0.7.x to v0.8.0

### Coordinate & Dimension System (`VPPos` / `VPSize` vs `CPos` / `CSize`)

In `v0.7.x`, the coordinate system used a single set of types (`Pos`, `Size`, `ColIndex`,
`RowIndex`, `ColWidth`, `RowHeight`) backed by 16-bit integers (`ChUnit` / `u16`). This
conflated physical screen window coordinates with backing buffer storage. Because storage
buffers (such as editor lines and terminal scrollback history) require `usize` addressing,
applications frequently required lossy or unchecked casts between `usize` and `u16`,
creating subtle truncation errors and preventing 2D panning across wide buffers. This also
constrained buffer addressing to 16-bit limits (maximum 65,535 lines and columns),
preventing the editor component and terminal scrollback history from addressing beyond
65,535 rows or columns.

In `v0.8.0`, the coordinate system establishes a strict, type-safe separation between two
distinct domains, bridged by a sliding window camera:

1. **Canvas Domain (`CPos`, `CSize`, `CCol`, `CRow`, `CWidth`, `CHeight`)**: Absolute
   64-bit (`usize`) document storage coordinates. Used by underlying data structures
   (`GrowableBuffer`, `Flat2DArray`, `EditorBuffer`) representing document space and
   scrollback history. The Canvas represents the in-memory backing data (loaded from disk,
   network, or child PTY streams), structured as a 2D buffer or 2D projection over a
   contiguous 1D buffer (such as `Flat2DArray`).
2. **Viewport Domain & Sliding Camera (`VPPos`, `VPSize`, `Viewport`)**: 0-based 16-bit
   (`u16`) screen coordinates representing the visible terminal window used for layout,
   cursor movement, and rendering. The concrete `Viewport` struct links both domains by
   pairing an absolute Canvas origin (`origin_pos: CPos`) with a visible screen size
   (`size: VPSize`), acting as a movable camera that handles scrolling and 2D panning over
   the larger Canvas buffer.
3. **Type-Safe Domain Conversions & Projections**: Conversions and projections between
   Viewport (`u16`) and Canvas (`usize`) are handled through dedicated traits rather than
   manual math or raw `as` casts:
    - **Camera Projection (`ViewportToCanvasExt`, `CanvasToViewportExt`)**: Trait-based
      method overloading on `Viewport` via `viewport.to_canvas(...)` (Viewport -> Canvas)
      and `viewport.to_viewport(...)` (Canvas -> Viewport, returning `None` if scrolled
      off-screen).
    - **Camera Panning (`CanvasCameraExt`)**: Automatically adjusts `origin_pos` via
      `viewport.pan_to_keep_coord_in_view(...)` to keep target coordinates in view.
    - **Dimension Conversions**: Checked narrowing via `TryFrom<CSize> for VPSize` and
      infallible widening via `From<VPSize> for CSize`.
    - **Numeric Casting Traits**: `NarrowingCastToU16` and `WideningCastToUsize` enforce
      safe numeric casting without raw `as` expressions.

> 💡 **Deep Dive**: For an architectural overview, ASCII domain flow diagrams, and the
> decorator vs. newtype design rationale, see [`tui/src/core/coordinates/canvas/mod.rs`].

#### Migration Directives & Symbol Mapping

When migrating code from `v0.7.x` that uses `row`, `col`, `pos`, `size`, `RowIndex`,
`ColIndex`, `ColWidth`, `RowHeight`, use this simple rule of thumb:

> 💡 **Rule of Thumb**: For screen-related units, use `VP_*` (`u16`). For memory-related
> units, use `C_*` (`usize`).

1. **1:1 Symbol Replacement Quick Reference**:

| v0.7.x Symbol          | Meaning in v0.7.x       | Screen Target (Viewport, `u16`)                     | Storage Target (Canvas, `usize`)                 |
| :--------------------- | :---------------------- | :-------------------------------------------------- | :----------------------------------------------- |
| `Pos`                  | 2D position struct      | `VPPos`                                             | `CPos`                                           |
| `Size`                 | 2D size struct          | `VPSize`                                            | `CSize`                                          |
| `ColIndex`             | 1D column position      | `VPCol`                                             | `CCol`                                           |
| `RowIndex`             | 1D row position         | `VPRow`                                             | `CRow`                                           |
| `ColWidth`             | 1D column width         | `VPWidth`                                           | `CWidth`                                         |
| `RowHeight`            | 1D row height           | `VPHeight`                                          | `CHeight`                                        |
| `col(c)`               | Column constructor      | `vp_col(c)`                                         | `c_col(c)`                                       |
| `row(r)`               | Row constructor         | `vp_row(r)`                                         | `c_row(r)`                                       |
| `width(w)`             | Width constructor       | `vp_width(w)`                                       | `c_width(w)`                                     |
| `height(h)`            | Height constructor      | `vp_height(h)`                                      | `c_height(h)`                                    |
| `col(c) + row(r)`      | 2D position constructor | `vp_pos(c, r)` _(or `vp_col(c) + vp_row(r)`)_       | `c_pos(c, r)` _(or `c_col(c) + c_row(r)`)_       |
| `width(w) + height(h)` | 2D size constructor     | `vp_size(w, h)` _(or `vp_width(w) + vp_height(h)`)_ | `c_size(w, h)` _(or `c_width(w) + c_height(h)`)_ |

2. **Replacing Manual Scroll Math (`ScrOfs`) with `Viewport` Projections**:
    - In `v0.7.x`, mapping between screen cursor and buffer content was typically done
      with manual addition or subtraction (e.g.,
      `let screen_row = buffer_row - scroll_offset;`).
    - In `v0.8.0`, eliminate `ScrOfs` and manual math:
        - Wrap the visible screen window and scroll origin in a `Viewport`:
          `let viewport = Viewport::new(origin_pos, size);`.
        - Project screen coordinates to backing buffer:
          `let abs_cpos = viewport.to_canvas(vp_pos);`.
        - Project backing buffer coordinates to visible screen:
          `let maybe_vppos = viewport.to_viewport(c_pos);` (returns `None` if scrolled out
          of the visible window).

#### Before (v0.7.x):

```rust
use r3bl_tui::{
    col, height, row, width, ColIndex, ColWidth, Pos, RowHeight, RowIndex, Size,
};

// In v0.7.x, a single 16-bit coordinate system was used for both screen and buffer:
let pos = Pos {
    col_index: ColIndex::from(10),
    row_index: RowIndex::from(5),
};
// Or via ergonomic operator addition:
let pos = col(10) + row(5);

let size = Size {
    col_width: ColWidth::from(80),
    row_height: RowHeight::from(24),
};
// Or via ergonomic operator addition:
let size = width(80) + height(24);
```

#### After (v0.8.0):

```rust
use r3bl_tui::{
    // Viewport coordinates (screen-relative, 0-based u16):
    VPCol, VPHeight, VPPos, VPRow, VPSize, VPWidth,
    vp_col, vp_height, vp_pos, vp_row, vp_size, vp_width,
    // Canvas coordinates (storage/document-relative, usize):
    CCol, CHeight, CPos, CRow, CSize, CWidth,
    c_col, c_height, c_pos, c_row, c_size, c_width,
    // Viewport camera and projection traits:
    CanvasToViewportExt, Viewport, ViewportToCanvasExt,
    // Explicit casting traits:
    NarrowingCastToU16, WideningCastToUsize,
};

// Scenario A: If migrating screen / window / layout / rendering code -> use Viewport (u16):
let vp_pos = vp_pos(10, 5);
// Or via struct initialization:
let vp_pos = VPPos {
    col_index: vp_col(10),
    row_index: vp_row(5),
};
// Or via operator addition:
let vp_pos = vp_col(10) + vp_row(5);

let vp_size = vp_size(80, 24);
// Or via struct initialization:
let vp_size = VPSize {
    col_width: vp_width(80),
    row_height: vp_height(24),
};
// Or via operator addition:
let vp_size = vp_width(80) + vp_height(24);

// Scenario B: If migrating document backing store / buffer / scrollback -> use Canvas (usize):
let c_pos = c_pos(100, 50);
// Or via struct initialization:
let c_pos = CPos {
    col_index: c_col(100),
    row_index: c_row(50),
};
// Or via operator addition:
let c_pos = c_col(100) + c_row(50);

let c_size = c_size(200, 1000);
// Or via struct initialization:
let c_size = CSize {
    col_width: c_width(200),
    row_height: c_height(1000),
};
// Or via operator addition:
let c_size = c_width(200) + c_height(1000);

// Scenario C: If migrating scroll offset math (formerly ScrOfs) -> use Viewport projections:
let viewport = Viewport::new(c_pos(10, 20), vp_size);

// Project Viewport -> Canvas (accounting for viewport origin):
let abs_cpos = viewport.to_canvas(vp_pos); // -> c_pos(20, 25)

// Project Canvas -> Viewport (returns None if scrolled off-screen):
let maybe_vppos = viewport.to_viewport(abs_cpos); // -> Some(vp_pos(10, 5))

// Explicit dimension conversions & numeric casting:
let converted_c_size: CSize = vp_size.into(); // Infallible widening (u16 -> usize)
let converted_vp_size: Result<VPSize, _> = c_size.try_into(); // Checked narrowing (usize -> u16)

let col_u16: u16 = c_pos.col_index.as_u16_narrowing();
let col_usize: usize = vp_pos.col_index.as_usize();
```

### App Trait Implementation

In `v0.8.0`, the `App` trait lifecycle separates component registration from background
service orchestration and simplifies the rendering return type:

1. **`app_init` becomes `app_init_components`**: Clarifies that this hook is exclusively
   responsible for populating the `ComponentRegistryMap` and setting the initial
   `HasFocus`.
2. **`app_start_background_services` introduces background worker orchestration**: A new
   lifecycle hook invoked once immediately after component initialization, before the
   first render. It provides access to `&mut GlobalData<Self::S, Self::AS>`, allowing apps
   to obtain `global_data.main_thread_channel_sender` to launch background Tokio tasks
   that dispatch signals back to the main thread. It has a default empty implementation
   `{}`, making it entirely opt-in.
3. **`app_render` returns `CommonResult`**: In `v0.7.x`, `app_render` returned
   `CommonResult<RenderPipeline>`. In `v0.8.0`, rendering pipelines are managed directly
   through `GlobalData` and `Surface`, simplifying the signature to return `CommonResult`
   (i.e. `ok!()` or `Ok(())`).

#### Before (v0.7.x):

```rust
use r3bl_tui::{
    App, CommonResult, ComponentRegistryMap, EventPropagation, GlobalData,
    HasFocus, InputEvent, RenderPipeline,
};

impl App for MyApp {
    type S = State;
    type AS = AppSignal;

    // In v0.7.x, the method was named app_init:
    fn app_init(
        &mut self,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) {
        // Initialize components and populate registry...
    }

    fn app_handle_input_event(
        &mut self,
        input_event: InputEvent,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult<EventPropagation> {
        Ok(EventPropagation::Propagate)
    }

    fn app_handle_signal(
        &mut self,
        signal: &Self::AS,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult<EventPropagation> {
        Ok(EventPropagation::Propagate)
    }

    // In v0.7.x, app_render was required to return RenderPipeline:
    fn app_render(
        &mut self,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult<RenderPipeline> {
        let mut surface = ...;
        // Render components into surface...
        Ok(surface.render_pipeline)
    }
}
```

#### After (v0.8.0):

```rust
use r3bl_tui::{
    App, CommonResult, ComponentRegistryMap, EventPropagation, GlobalData,
    HasFocus, InputEvent, ok,
};

impl App for MyApp {
    type S = State;
    type AS = AppSignal;

    // 1. Component initialization (renamed from app_init):
    fn app_init_components(
        &mut self,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) {
        // Initialize components and populate registry...
    }

    // 2. New lifecycle hook for background tasks (opt-in, default empty impl):
    fn app_start_background_services(
        &mut self,
        global_data: &mut GlobalData<Self::S, Self::AS>,
    ) {
        let sender = global_data.main_thread_channel_sender.clone();
        tokio::spawn(async move {
            // Spawn background workers that send signals back to main event loop...
        });
    }

    fn app_handle_input_event(
        &mut self,
        input_event: InputEvent,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult<EventPropagation> {
        Ok(EventPropagation::Propagate)
    }

    fn app_handle_signal(
        &mut self,
        signal: &Self::AS,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult<EventPropagation> {
        Ok(EventPropagation::Propagate)
    }

    // 3. Simplified return type: CommonResult instead of CommonResult<RenderPipeline>:
    fn app_render(
        &mut self,
        global_data: &mut GlobalData<Self::S, Self::AS>,
        component_registry_map: &mut ComponentRegistryMap<Self::S, Self::AS>,
        has_focus: &mut HasFocus,
    ) -> CommonResult {
        // Render components into surface; pipeline is managed automatically...
        ok!() // or Ok(())
    }
}
```

### Terminal Modes & `RenderOpCommon`

In `v0.7.x`, global terminal modes (raw mode, alternate screen, mouse capture, bracketed
paste) were queued into `RenderPipeline` as `RenderOpCommon` variants. In `v0.8.0`, all 10
mode variants are removed from `RenderOpCommon` and managed directly on `OutputDevice` via
the `TerminalModeController` trait with poison-safe RAII guards (`RawModeGuard` and
`FullScreenTuiModeGuard`).

This new architecture eliminates the **Double Panic Abort** risk (for deep dives, see
[`tui/README.md`] and [`tui/src/lib.rs`]): if your application panics, the RAII guards
safely recover poisoned output locks to restore cooked mode, re-enable the cursor, and
exit the alternate screen, preventing the user's terminal from being left bricked.

> 💡 **Note**: If your app uses `TerminalWindow::main_event_loop()`, terminal mode setup
> and teardown are handled automatically. You only need this migration if you manage
> terminal modes manually.

#### Before (v0.7.x):

```rust
// In v0.7.x, global terminal states were queued into the render pipeline:
pipeline.push(RenderOpCommon::EnterRawMode);
pipeline.push(RenderOpCommon::EnableMouseCapture);
pipeline.push(RenderOpCommon::EnterAlternateScreen);
pipeline.push(RenderOpCommon::HideCursor);
```

#### After (v0.8.0):

```rust
use r3bl_tui::{OutputDevice, TerminalModeController};

let mut output_device = OutputDevice::new_stdout();

// Recommended: Use RAII guards for automatic, panic-safe cleanup:
let _raw_mode_guard = output_device.enter_raw_mode()?;
let _fullscreen_guard = output_device.setup_full_screen_tui()?;
// All terminal modes (alternate screen, cursor, mouse tracking, raw mode)
// are automatically restored when guards drop, even during panics!

// Or manage individual modes granularly via TerminalModeController:
output_device.enable_mouse_tracking()?;
output_device.disable_mouse_tracking()?;
output_device.show_cursor()?;
output_device.hide_cursor()?;
```

### PTY Sessions & Multiplexing

In `v0.7.x`, PTY sessions were strictly asynchronous and required an active Tokio runtime
context. However, low-level OS pseudo-terminal interfaces (such as Windows ConPTY and Unix
PTY streams) are fundamentally blocking `std::io::Read` and `std::io::Write` operations.
Forcing an async abstraction onto blocking OS calls added unnecessary executor overhead
(via `spawn_blocking` and `blocking_send` bridges) and prevented developers from using PTY
sessions in synchronous binaries, worker threads, scripts, or lightweight test harnesses
without spinning up a full Tokio reactor.

In `v0.8.0`, the PTY architecture follows the principle of respecting the synchronous core
and adding an async adapter for async use cases. For a deep dive into this design
philosophy, see [To async or not to async: Rust MCP server].

1. **Synchronous Core Engine (`start()`)**: `PtySessionBuilder::start()` returns a purely
   synchronous `PtySession` powered by standard OS threads (`pty-reader`, `pty-writer`,
   `pty-orchestrator`) and `std::sync::mpsc::sync_channel`. This removes the Tokio runtime
   dependency completely for synchronous workloads, scripts, and tests.
2. **Async Adapter (`start_async()`)**: For applications with async event loops (such as
   interactive TUI apps), `PtySessionBuilder::start_async()` returns an `AsyncPtySession`
   backed by Tokio channels and bridge tasks for direct integration with `tokio::select!`.

#### Before (v0.7.x):

```rust
use r3bl_tui::core::pty::{
    DefaultPtySessionConfig, PtySession, PtySessionBuilder, PtySessionConfigOption,
};

// In v0.7.x, start() returned an async PtySession coupled to Tokio:
let mut session: PtySession = PtySessionBuilder::new("htop")
    .with_config(DefaultPtySessionConfig + PtySessionConfigOption::Size(terminal_size))
    .start()?;

// In async event loop:
tokio::select! {
    Some(event) = session.rx_output_event.recv() => { /* ... */ }
}
```

#### After (v0.8.0):

```rust
use r3bl_tui::core::pty::{
    AsyncPtySession, DefaultPtySessionConfig, PtySession, PtySessionBuilder, PtySessionConfigToken,
};

// 1. For Tokio async event loops: use start_async() to get an AsyncPtySession
let mut async_session: AsyncPtySession = PtySessionBuilder::new("htop")
    .with_config(DefaultPtySessionConfig + PtySessionConfigToken::Size(terminal_size))
    .start_async()?;

tokio::select! {
    Some(event) = async_session.rx_output_event.recv() => { /* ... */ }
}

// 2. For synchronous binaries/scripts (no Tokio runtime needed): use start() to get a PtySession
let mut sync_session: PtySession = PtySessionBuilder::new("htop")
    .with_config(DefaultPtySessionConfig + PtySessionConfigToken::Size(terminal_size))
    .start()?;

while let Ok(event) = sync_session.rx_output_event.recv() { /* ... */ }
```

## 📄 Full Changelog

- [r3bl_tui v0.8.0 Changelog Entry]

<!-- Ref Def Links -->

[Migration Guide]: #migration-guide-from-v07x-to-v080
[Interactive Demos]: #interactive-demos
[crates.io]: https://crates.io/crates/r3bl_tui
[docs.rs]: https://docs.rs/r3bl_tui/
[`tui/README.md`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/README.md#the-double-panic-abort-risk
[`tui/src/lib.rs`]: https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/lib.rs
[`tui/src/core/coordinates/canvas/mod.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/core/coordinates/canvas/mod.rs
[`Node.js`]: https://nodejs.org/
[`React`]: https://react.dev/
[`ink`]: https://developerlife.com/2021/11/25/ink-v3-advanced-ui-components/
[React]: https://react.dev/
[flexbox]:
    https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Basic_concepts_of_flexbox
[CSS]: https://www.w3.org/TR/CSS/#css
[`vim`]: https://www.vim.org/
[`neovim`]: https://neovim.io/
[`ratatui`]: https://ratatui.rs/
[Elm]: https://guide.elm-lang.org/architecture/
[unidirectional data flow]: https://guide.elm-lang.org/architecture/
[responsive]:
    https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design
[declarative]: https://ui.dev/imperative-vs-declarative-programming
[CSS-like]: https://www.w3.org/TR/CSS/#css
[`tui/examples`]: https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples
[`readline_async`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/readline_async/readline_async_api.rs
[`readline_async.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/readline_async.rs
[`readline_async_multiline.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/readline_async_multiline.rs
[`spinner.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/spinner.rs
[`shell_async.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/shell_async.rs
[`choose_interactive.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/choose_interactive.rs
[`choose_quiz_game.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/choose_quiz_game.rs
[`choose_with_and_without_readline_async.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/choose_with_and_without_readline_async.rs
[`tui_apps`]: https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps
[`ex_editor`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_editor
[`ex_pitch`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_pitch
[`ex_rc`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_rc
[`ex_app_with_1col_layout`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_app_with_1col_layout
[`ex_app_with_2col_layout`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_app_with_2col_layout
[`ex_app_no_layout`]:
    https://github.com/r3bl-org/r3bl-open-core/tree/main/tui/examples/tui_apps/ex_app_no_layout
[`pty_mux_example.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_mux_example.rs
[`pty_2d_panning_example.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_2d_panning_example.rs
[`pty_simple_example.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_simple_example.rs
[`pty_rw_echo_example.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_rw_echo_example.rs
[`pty_mux_osc_demo.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_mux_osc_demo.rs
[`spawn_pty_interactive.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/spawn_pty_interactive.rs
[`spawn_pty_output_capture.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/spawn_pty_output_capture.rs
[`mouse_inspector.rs`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/mouse_inspector.rs
[Build with Naz: TTY playlist]:
    https://www.youtube.com/playlist?list=PLofhE49PEwmw3MKOU1Kn3xbP4FRQR4Mb3
[Build with Naz: async readline]:
    https://www.youtube.com/playlist?list=PLofhE49PEwmwelPkhfiqdFQ9IXnmGdnSE
[API documentation on docs.rs]: https://docs.rs/r3bl_tui/
[Parse, don't validate]:
    https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/
[High-Performance Flat 2D Arrays in Rust (SIMD, L1 Cache)]:
    https://developerlife.com/2026/07/14/build-high-performance-flat-2d-arrays-in-rust/
[Rust, Memory performance & latency]:
    https://developerlife.com/2025/05/19/rust-mem-latency/
[To async or not to async: Rust MCP server]:
    https://developerlife.com/2026/08/22/to-async-or-not-to-async-rust-mcp-server/
[r3bl_tui v0.8.0 Changelog Entry]:
    https://github.com/r3bl-org/r3bl-open-core/blob/v0.8.0-tui/CHANGELOG.md#v080-2026-09-18
[curses]: https://en.wikipedia.org/wiki/Curses_(programming_library)
[`Shift+Enter`]:
    https://docs.anthropic.com/en/docs/agents-and-tools/claude-code/overview#terminal-setup
[Kitty keyboard protocol]: https://github.com/vadimdemedes/ink/pull/855
[`PTYMux`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/core/pty/pty_mux/mux.rs
[`GrowableBuffer`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/tui/terminal_lib_backends/ofs_buf/storage/impls/growable_buffer.rs
[`pty_mux_example`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/examples/pty_mux_example.rs
[`readline`]: https://man7.org/linux/man-pages/man3/readline.3.html
[loosely coupled and strongly coherent]:
    https://developerlife.com/2015/11/05/loosely-coupled-strongly-coherent/
[`choose`]:
    https://github.com/r3bl-org/r3bl-open-core/blob/main/tui/src/readline_async/choose_api.rs
[`tmux`]: https://github.com/tmux/tmux
[FUNARCH 2023 paper]: https://dl.acm.org/doi/epdf/10.1145/3609025.3609477
[FUNARCH 2026 paper]: https://dl.acm.org/doi/epdf/10.1145/3830438.3830958
[make illegal states unrepresentable]: https://corrode.dev/blog/illegal-state/

<!-- cspell:words cpos vppos -->